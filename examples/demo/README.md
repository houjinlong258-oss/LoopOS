# Founding Preview Demo Inputs

This directory contains inert inputs for the offline flows in `docs/demo-flows.md`.

- `policy-bypass.diff` is a unified diff used to demonstrate the Maintainability Gate.
- It is text only and is never executed.

Run:

```bash
loopos code gate --diff examples/demo/policy-bypass.diff
```
